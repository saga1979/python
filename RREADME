【运行】
python3 ./log_monitor.py -c [配置文件路径]

配置文件参数是可选的，如果不指定将采用程序内置的配置，
而且会将内置配置写入当前目录下的app_config.json文件。
所以使用时可以先不带配置参数，运行后修改其生成的配置文件。
对于变名文件、数据表，支持采用python语法修改对应的"file"、"table"字段

【测试】
socket_server.py 为测试用服务器，简单的接受并显示消息

【备注】
1. 对日志文件的分析需要触发文件修改事件
2. 程序正常结束（比如按Ctrl+C）会记录文件、数据库的读取位置
3. 程序为单例程序，可以通过Linux系统下的cron配置为系统服务
4. 目前为调试阶段，默认程序自身记录日志的级别为DEBUG
5. 发送信息中不少字段从日志中暂时找不到数据，先让程序能工作起来（TODO）
6. 多点登录虽然有配置，但处于禁用状态（没有数据）
